package main

import "fmt"

func main() {
	var number int
	var name string
	var boolean bool
	var floating_point float32
	var floatingPoint float64

	fmt.Printf("Number %d,\n Name %s,\n Boolean %v,\n Floating points %v, %v\n", number, name, boolean, floating_point, floatingPoint)
}
